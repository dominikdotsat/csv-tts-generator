const assert = require('assert');
const csvTtsGenerator = require('../index.js');

describe('csvTtsGenerator', () => {
  it('has a test', () => {
    assert(false, 'csvTtsGenerator should have a test');
  });
});
